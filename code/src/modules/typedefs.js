/**
 * @module typedefs
 */
define(function(){
    /**
     * A stage is a function used as a single processing step within a pipeline
     * @typdef {Function} Stage
     * @memberof module:typedefs
     */
    var stage;
});
