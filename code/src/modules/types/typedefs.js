/**
 * @module types
 */
define(function(){
    /**
     * A stage is a function used as a single processing step within a pipeline
     * @typedef {Function} Stage
     * @memberof module:types
     */
    var stage;
});
